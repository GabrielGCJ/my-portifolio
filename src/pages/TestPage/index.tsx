import { Contact } from "../../components/Contact"
import { TestContainer } from "./styles"

export const TestPage = () => {
    return(
        <TestContainer>
            <Contact/>
        </TestContainer>
    )
}